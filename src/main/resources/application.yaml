server:
  port: 8082
spring:
  application:
    name: health-tracker-rest
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true